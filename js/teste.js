// const submit = document.querySelector('[wm-submit]')
// submit.onclick = function(e) {
//     e.preventDefault()

//     const form = e.target.parentNode
//     const formData = new FormData(form)
//     formData.append('ajax', true)
//     console.log(formData.get('cep'))
//     console.log(formData.has('data'))

//     for (let dado of formData) {
//         console.log(dado)
//     }
// }




/*
const submit = document.querySelector('[wm-submit]')
submit.onclick = function(e) {
        e.preventDefault()
        const form = e.target.parentNode
        const formData = new FormData(form)
        formData.append('ajax', true)

        const inputValue = form.pesquisa.value;

        
        const petShop = document.getElementsById.true('pet')
        petShop = '+pet+shop/data=!4m4!2m3!5m1!2e1!6e6'

        const clinicaVeterinaria = document.getElementsById.true('vet')
        clinicaVeterinaria = '+clinica+veterin%C3%A1ria/data=!4m3!2m2!5m1!2e1'

        const elemento = elemento.appendChild(data ? petShop : clinicaVeterinaria)

        
        location.href = document.getElementById('pesquisa')  + inputValue + elemento;

       

}
*/




// const form = document.forms[0]; // Cria referência para o formulário

// function submit(event){
//     event.preventDefault(); // Previne o comportamento padrão do botão submit

//     const inputValue = form.busca.value; // Obtém o valor do campo de texto

//     location.href = "http://google.com.br/" + inputValue; // Concatena a URL do Google com o valor do campo de texto e redireciona a página
// }

// form.submit.addEventListener("click", submit); // Espera o usuário clicar no botão submit, para executar a função submit();



